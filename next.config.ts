import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Logical properties enforced in CSS — no special Next config needed
  // Strict mode enabled for early detection of issues
  reactStrictMode: true,
};

export default nextConfig;
